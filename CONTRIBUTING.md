# Contributing to Shammy

1. Fork and clone the repo.
2. Run `pnpm install`.
3. Use `./scripts/dev/start.sh` to run the stack.
4. Follow the style enforced by ESLint and Prettier.
5. Ensure `pnpm run lint` and `pnpm run build` pass before opening a PR.
